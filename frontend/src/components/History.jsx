// import React from "react";

// const History = ({ history, toggleComplete }) => {
//   return (
//     <div className="history">
//       <h2>Completed Tasks</h2>
//       {history.length === 0 && <p>No completed tasks yet</p>}
//       {history.map((task) => (
//         <div key={task._id} className="todo-item completed">
//           <p>{task.task}</p>
//           <button onClick={() => toggleComplete(task._id)}>Undo</button>
//         </div>
//       ))}
//     </div>
//   );
// };

// export default History;
