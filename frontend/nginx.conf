# server {
#     listen 80;
#     server_name jettyjaaaaa.space www.jettyjaaaaa.space;

#     # Redirect root URL to /todolistapp/
#     location = / {
#         return 301 /todolistapp/;
#     }

#     # Serve frontend from Docker container (Nginx)
#     location /todolistapp/ {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri /index.html;

#         location /todolistapp/assets/ {
#             root /usr/share/nginx/html;
#             expires 6M;
#             access_log off;
#             add_header Cache-Control "public";
#         }
#     }

#     # Proxy requests to backend API
#     location /api/ {
#         proxy_pass http://localhost:3001/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Cache static files for better performance
#     location ~* \.(?:ico|css|js|gif|jpe?g|png|woff2?|eot|ttf|otf|svg|mp4|webm)$ {
#         expires 6M;
#         access_log off;
#         add_header Cache-Control "public";
#     }

#     # Block access to hidden files
#     location ~ /\. {
#         deny all;
#     }
# }
